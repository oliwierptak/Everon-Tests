; Everon domain configuration example

[default]
id_field = id
type = table
connection[read] = read
connection[write] = write

['Foo < default']
table = foo

['Bar < default']
table = bar

['User < default']
table = s_users.user

['Ticket < default']
table = s_misc.ticket